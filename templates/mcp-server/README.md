# SERVICE_NAME MCP server (template)

Generated by agent-terraform's `generate-mcp` skill from
`services/SERVICE_NAME/spec.yaml`. Do not edit by hand — change the spec
and regenerate.

## Run

```bash
export SERVICE_BASE_URL_ENV=https://service.internal.example
export SERVICE_API_KEY_ENV=...   # omit if the service uses network trust
uv run --directory serving/mcp/SERVICE_NAME fastmcp run server.py
```

## Register with Claude Code

```bash
claude mcp add SERVICE_NAME -- uv run --directory serving/mcp/SERVICE_NAME fastmcp run server.py
```

## Tools

<generated: table of tool name · safety · one-line description>
